jest.unmock("../reducerUtils");

import { mapStateToProps } from "../reducerUtils";

describe("AssignableAttendeeListContainer", () => {
    it("can add", () => {
        expect(2 + 2).toBe(4);
    });
});