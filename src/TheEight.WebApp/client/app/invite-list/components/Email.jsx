export default function(props) {
    return (
        <div>
            <input type='hidden' />
            <input type='text' />
            <select>
                <option value=''>

                </option>
            </select>
            <button onClick={}>
                Delete
            </button>
        </div>
    );
}