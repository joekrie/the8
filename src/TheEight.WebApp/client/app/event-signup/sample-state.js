import { List, Map } from "immutable";

import EventDetailsRecord from "../common/models/event-details-record";
import { PRACTICE_MODE } from "../common/models/event-modes";

const sampleState = {
  events: [
    
  ]
};

export default sampleState;