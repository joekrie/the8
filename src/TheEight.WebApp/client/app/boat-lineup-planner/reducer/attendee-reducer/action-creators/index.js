import { 
  ADD_ATTENDEE,
  ADD_BOAT,
  SET_INITIAL_STATE
} from "../actions"

export const addAttendee = createAction(ADD_ATTENDEE)
export const addBoat = createAction(ADD_BOAT)
export const setInitialState = createAction(SET_INITIAL_STATE)
